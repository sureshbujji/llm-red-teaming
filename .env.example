# Placeholder keys only — this project runs with NO api key.
# Copy to .env if you later swap the mock target for a real model endpoint.
TARGET_API_URL=
TARGET_API_KEY=<redacted>
TARGET_MODEL=mock
JUDGE_MODEL=deterministic
